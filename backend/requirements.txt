fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Phase 1 (MIDI Analyzer)
pretty_midi==0.2.10
mido==1.3.2
setuptools<81  # pretty_midi imports pkg_resources at runtime; removed from setuptools 81+

# Phase 2 (Lyric Lab) — deterministic rhyme/syllable analysis via the CMU Pronouncing Dictionary
pronouncing==0.3.0
cmudict==1.1.3

# Phase 3 (AI Coach)
# Unpinned: numpy==1.26.4 has no Windows/py3.13 wheel and fails to build without a C compiler.
# Let pip resolve versions with prebuilt wheels for the installed Python instead.
librosa>=0.10.2
soundfile>=0.12.1
numpy>=1.26

# Phase 4 (Polish) — history persistence
aiosqlite==0.22.1
